{% doc %}
  Product title (h1).
{% enddoc %}

<h1 class="product-info__title" {{ block.shopify_attributes }}>{{ product.title }}</h1>

{% schema %}
{
  "name": "Product title",
  "settings": [],
  "presets": [{ "name": "Product title" }]
}
{% endschema %}
